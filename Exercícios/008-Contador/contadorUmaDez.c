#include <stdio.h>

int main(){
    int i = 1; // Inicializa o contador i com 1

    // Estrutura while que vai contar de 1 até 10
    while (i <=10)
    {
        printf("%d\n", i);  // Imprime o valor de i
        i++;  // Incrementa i em 1
    }
    
    return 0;  // Fim do programa
}